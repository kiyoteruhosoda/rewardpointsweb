[project]
name = "rewardpointsweb"
version = "0.2.0"
description = "RewardPoints — 人ごとのポイントを記録・共有する PWA（FastAPI + DDD）"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.47.0",
    "gunicorn>=23.0.0",
    "prometheus-fastapi-instrumentator>=7.1.0",
    "sqlalchemy>=2.0.36",
    "alembic>=1.14.0",
    "pymysql>=1.1.0",
    "cryptography>=44.0.0",
    "pyjwt>=2.10.0",
    "werkzeug>=3.0.0",
    "pydantic[email]>=2.7.0",
    "python-dotenv>=1.0.0",
    "pyotp>=2.9.0",
    "webauthn>=2.5.0",
    "qrcode>=8.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "mypy>=1.14",
    "pillow>=12.3.0",
    "pytest>=9.0.3",
    "ruff>=0.11.0",
    "types-qrcode>=8.2.0.20260518",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths  = ["tests"]
# importlib: 同名テストファイルの衝突回避 / smtp: 外部リソース要のため既定除外
addopts = "--import-mode=importlib -m 'not smtp'"
markers = [
    "smtp: requires a real SMTP server (excluded by default)",
]

[tool.mypy]
python_version = "3.12"

# 検査対象。この構成に "src/" は無いため、実際のパッケージルートを列挙する
# （引数なしの `uv run mypy` がそのまま品質ゲートになる）。
files = [
    "asgi.py",
    "main.py",
    "bounded_contexts",
    "migrations",
    "presentation",
    "scripts",
    "shared",
    "tests",
]

strict = true

pretty = true
show_error_codes = true
show_column_numbers = true

disallow_any_generics = true
disallow_subclassing_any = true

disallow_untyped_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true

no_implicit_optional = true

warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true

implicit_reexport = false

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle: PEP8 準拠
    "F",    # Pyflakes: 未使用変数・import
    "I",    # isort: import 順
    "B",    # flake8-bugbear: バグになりやすい記述
    "UP",   # pyupgrade: 古い Python 構文の検出
    "SIM",  # flake8-simplify: 不要な分岐の簡略化
    "C4",   # flake8-comprehensions: comprehension 改善
    "ARG",  # flake8-unused-arguments: 未使用引数
    "N",    # pep8-naming: 命名規則
    "RET",  # flake8-return: return 改善
    "PTH",  # flake8-use-pathlib: pathlib 利用推奨
    "RUF",  # Ruff 独自ルール
    # 設計品質の定量基準（ADR-0008）。閾値は [tool.ruff.lint.mccabe] /
    # [tool.ruff.lint.pylint] に置く。
    "C901",    # 複雑度
    "PLR0912", # 分岐の数
    "PLR0913", # 引数の数
    "PLR0915", # 文の数（関数長の代替）
]

ignore = [
    # FastAPI の Depends()/Cookie()/Query() は引数デフォルトでの呼び出しが正しい用法
    "B008",
    # 本プロジェクトのコメント・docstring は日本語であり、全角括弧・読点は意図的な表記。
    # RUF001-003（ambiguous-unicode-character）は誤検知にしかならないため除外する。
    "RUF001",
    "RUF002",
    "RUF003",
]

[tool.ruff.lint.mccabe]
# 開発標準どおり。導入時点の違反は 0 件（ADR-0008）。
max-complexity = 10

[tool.ruff.lint.pylint]
# 分岐と文の数。関数長 30 行そのものを見るルールは Ruff に無いため、
# 文の数（30）で近似する。導入時点の違反はいずれも 0 件（ADR-0008）。
max-branches = 8
max-statements = 30
# 引数は開発標準の 3 ではなく 5。PLR0913 はキーワード専用引数と FastAPI の
# Depends() も 1 個として数えるため、3 にすると呼び出し側の読みやすさを
# 損なっていない箇所まで落ちる。位置引数 3 個までは引き続きレビュー観点
# （理由と計測結果は ADR-0008）。
max-args = 5

[tool.ruff.lint.per-file-ignores]
# ARG001: pytest フィクスチャは引数として受け取るだけで参照しないことがある
# ARG002: テストダブルはインターフェースを満たすために使わない引数も宣言する
"tests/*" = ["ARG001", "ARG002"]
